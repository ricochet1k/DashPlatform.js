/**
 * @param {ArrayBuffer | Uint8Array} arrayBuffer
 */
export function toHex(arrayBuffer: ArrayBuffer | Uint8Array): string;
/**
 * @param {string} string
 */
export function fromHex(string: string): Uint8Array<ArrayBuffer>;
